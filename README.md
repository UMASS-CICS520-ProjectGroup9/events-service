# Events Service

This service manages events for the UMASS project. It is a Django project.

## Getting Started

To run this service:

1.  Install the dependencies:
    ```bash
    pip install -r requirements.txt
    ```
2.  Run the development server:
    ```bash
    cd eventsService
    python manage.py runserver
    ```

